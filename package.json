{
  "name": "revue-components",
  "version": "0.2.0",
  "description": "Personal Vue reuseable components",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "izitoast": "^1.4.0",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "axios": "^0.21.0"
  }
}
